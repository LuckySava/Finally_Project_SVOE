{
  "scripts": {
    "build": "rm -rf dist && parcel build ./src/index.html —public-url Finally_Project_SVOE",
    "start": "rm -rf dist .cache && parcel src/index.html -p 3000",
    "publish": "gh-pages -d dist"
  },
  "name": "svoe",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "keywords": [],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "cssnano": "^4.0.4",
    "gh-pages": "^1.2.0",
    "parcel-plugin-sw-precache": "^0.5.2"
  }
}
